spring:
  application:
    name: sample-event-sourcing-mysql
  profiles:
    active: default

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
